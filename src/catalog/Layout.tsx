const Layout = () => {
    return (
        <div>
            <h1 className="transition-transform duration-300 delay-500 hover:scale-110">
                — Free shipping on orders $100+
            </h1>
        </div>
    );
};

export default Layout;
